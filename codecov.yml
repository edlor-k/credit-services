coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        informational: true
    patch:
      default:
        target: 80%
        threshold: 0%
        informational: true

comment:
  layout: "reach, diff, flags, files"
  require_changes: false
  behavior: default
